version: '3'
networks:
  my-network:
    driver: bridge
services:
  web_api:
    image: web_identity_dev
    container_name: web-api
    networks:
      - my-network
  nginx:
    image: mynginx_dev
    container_name: nginx-proxy
    ports:
      - "8000:80"
      - "8001:443"
    networks:
      - my-network